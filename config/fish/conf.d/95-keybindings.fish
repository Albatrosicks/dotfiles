# Fish uses emacs-style bindings by default
# Most of these are already built-in, but here are the explicit bindings

bind \ck kill-whole-line # ctrl-k
bind \cr history-pager # ctrl-r (fish's history search)
bind \ca beginning-of-line # ctrl-a
bind \ce end-of-line # ctrl-e
bind \cp up-or-search # ctrl-p
bind \cy accept-autosuggestion # ctrl-y (fish equivalent)
bind \cn history-token-search-forward # ctrl-n (fish equivalent)
bind \cd delete-char # ctrl-d
bind \cf forward-char # ctrl-f
bind \cb backward-char # ctrl-b
