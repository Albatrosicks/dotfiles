# Fish has built-in autocd functionality
# Set directory stack size (fish uses dirh/dirs)
set -U fish_prompt_pwd_dir_length 5

# Fish has extended globbing by default
# Fish doesn't have nomatch issues like zsh
